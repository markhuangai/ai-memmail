services:
  postgres:
    image: postgres:18
    environment:
      POSTGRES_DB: ai_memmail
      POSTGRES_USER: ai_memmail
      POSTGRES_PASSWORD: ai_memmail
    ports:
      - "${AI_MEMMAIL_POSTGRES_PORT:-15432}:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ai_memmail -d ai_memmail"]
      interval: 5s
      timeout: 5s
      retries: 10

  app:
    image: ghcr.io/markhuangai/ai-memmail:latest
    environment:
      AI_MEMMAIL_CONFIG: /app/config/config.yaml
      AI_MEMMAIL_BIND: 0.0.0.0:8080
      CONTROL_PANEL_KEY: ${CONTROL_PANEL_KEY:?CONTROL_PANEL_KEY is required}
    ports:
      - "${AI_MEMMAIL_HTTP_PORT:-18080}:8080"
    volumes:
      - ./config/config.yaml:/app/config/config.yaml
      - ./prompts:/app/prompts:ro
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  postgres-data:
